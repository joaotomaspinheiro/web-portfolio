export enum Routes {
    MAIN = "main",
    ABOUT = "about",
    PROJECTS = "projects",
    CONTACT = "contact"
}

export enum AboutItems {
    HARD_SKILLS = "hard skills",
    SOFT_SKILLS = "soft skills"
}